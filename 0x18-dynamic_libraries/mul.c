#include <stdlib.h>
#include <stdio.h>

/**
 * mul - multiply  two integers 
 * @a: first int 
 * @b: second int 
 * Return: multiplication of the two integers
 */

int mul(int a, int b)
{
	return (a * b);
}
