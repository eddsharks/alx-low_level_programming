#include "main.h"

/**
 * main - Entry point
 *
 * Description: tests the positive_or_negative function
 * Return: Always 0 (Success)
 */
int main(void)
{
    int i;

    i = 98;
    positive_or_negative(i);

    return (0);
}
